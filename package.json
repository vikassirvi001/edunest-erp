{
  "name": "@caffeine/template-app",
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "workspaces": [
    "src/app"
  ],
  "scripts": {
    "build": "npm run build --workspace=@eduvault-prime/app --if-present",
    "typecheck": "npm run typecheck --workspace=@eduvault-prime/app --if-present",
    "check": "npm run check --workspace=@eduvault-prime/app --if-present",
    "test": "npm run test --workspace=@eduvault-prime/app --if-present",
    "fix": "npm run fix --workspace=@eduvault-prime/app --if-present"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
